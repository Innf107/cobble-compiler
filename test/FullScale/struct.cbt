--# definition and construction : score
struct S {
    x::Int
,   y::Bool
};

--! 5
let s = S{x=3,y=_true ()} in 5

--# getter : score
struct S {
    x::Int
,   y::Bool
};

--! 3
let s = S{x=3,y=_true ()} in s.x

--! 4
let s = S{x=3,y=_true ()} in if s.y then x else _add(x,1)

--# struct type : score
struct SomeS {
    x::Int
,   y::Int
};

s :: Int -> SomeS;
s v = SomeS {x=v, y=v};

--! 8
s.x + s.y

